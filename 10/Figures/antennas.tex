\feynmandiagram[small]
{
    a[particle=a]-- aa[dot] -- v[dot];
    b[particle=b]-- bb[dot] -- v;
    c[particle=c]-- cc[dot] -- v;

    aa -- ax[square dot];
    bb -- bx[square dot];
    cc -- cx[square dot];
};
